import React, { Component, useState } from 'react';

export default function UserSearchIcon() {
    return (<img src="content/icons/hiring-icon.svg" alt="" onDragStart={e => e.preventDefault()} />);
}